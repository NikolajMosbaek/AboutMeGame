// Single source of truth for the build/version marker shown on the title
// screen. Confirms end-to-end wiring at a glance and grows into a real
// build-stamp when CI lands.
export const APP_VERSION = "0.1.0";

// The product vision, surfaced as the title-screen tagline so the vision and
// the UI are validated together (real copy, not placeholder).
export const VISION =
  "A lightweight, browser-based social party game where a small group answers prompts about themselves and players score points by guessing each other's answers — no installs, just a shared link.";
