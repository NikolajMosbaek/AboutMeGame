/// <reference types="vite/client" />
// Pulls in Vite's ambient types — notably `import.meta.env` (BASE_URL, DEV,
// PROD), which `src/engine/assets.ts` uses to resolve asset URLs under the
// GitHub Pages sub-path.
