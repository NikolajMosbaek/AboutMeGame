# Run log — 2026-07-10 — Visual overhaul

## Intake

Owner directive (verbatim intent): *"I'm still not happy with the graphics. … research what
it takes for you to do a MAJOR graphics update in every way. The requirements are simply:
1. It must be free. 2. You can do it without any input from me."* Followed by: *"go ahead
and do the full upgrade."*

## Research summary (grounds the design)

Stack inventory (agent sweep, 2026-07-10): game is 100% procedural — every material
`MeshStandardMaterial` flat-shaded/vertex-colored, zero shipped texture/model binaries,
one directional + one hemisphere light, gradient-shader sky dome, two-sine water patch,
bloom-only compositor (three-examples chain), three ^0.169. Budget headroom: 216 KB of
400 KB JS gzip; ~0.25 MB of 6 MB initial download.

Tooling research: pmndrs `postprocessing` (MIT) + `n8ao` (MIT) for the effects stack;
CC0 assets from Poly Haven / ambientCG / Quaternius / Kenney; gltf-transform +
meshopt for model compression; three current release r185 (WebGPU exists but stays out of
scope — no visual payoff, real migration risk).

## Decisions

1. Ship as **7 sequential slice PRs directly to `main`** (not an integration branch): each
   slice is an unambiguous standalone upgrade of one subsystem; hybrid mid-states look
   fine, and production improves continuously. (Contrast with the pivot, where a
   half-pivoted hybrid was unacceptable.)
2. Design doc is binding: `docs/design/2026-07-10-visual-overhaul-design.md`.
3. Free-only policy enforced by an asset license manifest (`public/assets/LICENSES.md`)
   listing source + license for every imported binary.

## Trail

| Slice | PR | Result |
|---|---|---|
| 1 — foundation (three 0.185 + pmndrs post stack) | — | in progress; review finding fixed pre-PR: postprocessing was folded into the eager `three` chunk (low tier paid ~74 KB gz for nothing) → now a lazy `postfx` chunk behind a `quality.bloom`-gated dynamic import (`loadCompositor` seam), eager JS +7.6 KB (three's own growth only) |
| 2 — lighting (sky IBL + N8AO + shadows) | — | in progress; sky-driven PMREM environment light on ALL tiers (`EnvLightSystem`, built by `GameCanvas` — needs the real renderer `buildWorld`/`buildGame` never touch), regen scheduled by a pure palette-delta + real-seconds cap (`envBakeScheduler.ts`, ~every 2s while the sky is moving, measured over a full 180s loop; low bakes once at golden-hour and never again); retired the flat `HemisphereLight` (nothing else referenced it); N8AO ambient occlusion added inside the existing lazy `postfx` chunk (medium "Performance", high "Medium"; `+77.25 KB` gz to that chunk — the single biggest bundle cost of the slice, only 23.3 KB of the 400 KB JS-gzip cap left after it, flagged for the next slice); player-following texel-snapped shadow frustum (`ShadowFrustumSystem`, headless-tested, `lightBasis` proven bit-exact against `THREE.Matrix4.lookAt`) replaces the whole-island frame — same map size now ~3x sharper texels (not the ~10x a naive read of the halfExtent range might suggest; recorded as a deviation). All 4 gates green (`build`/`test`/`check:bundle`/`verify` EXIT=0); `--landmark-tour`'s accent-coverage check for 4/6 sites was independently confirmed to ALREADY fail on unmodified `main` (A/B tested with AO on/off — identical failure either way, plus a visual check showing the framing is simply too wide/distant to resolve the tiny accent prop) — a pre-existing gap outside this slice's scope, not a regression. review: sun-direction corruption off-origin — caught by review, fixed with a direction-owner seam. render-gate caught software-GL stall (CI's GPU-less runner resolved medium → N8AO + ~2s env rebakes on SwiftShader = seconds/frame, screenshot timeout); fixed with renderer-string tier override (`isSoftwareRenderer` in `deviceCapability.ts` forces detected tier low; explicit setting still wins). |
| 3 — terrain PBR splatting | — | 4 CC0 ambientCG texture sets (Grass001 jungle floor, Ground037 leaf litter, Rock057 mossy rock, Ground054 river-mud/sand — chosen by downloading and looking at the albedo previews, not guessed from tags; `public/assets/LICENSES.md`), processed by a new `scripts/process-textures.mjs` (sharp devDependency, build-time only) to 1024px WebP (albedo q80, normal q90), 2701.0 KB total. Splat weights are a pure, tested module (`src/world/terrainSplat.ts`) driven by the SAME height bands `colorForHeight` already used plus slope (from the now-smooth vertex normal) and noise; packed into a vec4 vertex attribute the terrain's `onBeforeCompile` patch (`terrainMaterialPatch.ts`, the `waterPatch.ts` idiom) blends 4 albedo (+4 normal, medium/high) samples by, world-XZ planar UV at 6 units/repeat. The surviving vertex-colour macro tint needed ZERO extra GLSL — it rides three's own `color_fragment` multiply, which runs after our albedo write by anchor ordering alone. `flatShading` dropped (now `computeVertexNormals`); `heightAt` and every gameplay-relevant sample stayed byte-identical (existing `terrain.test.ts` unchanged and green). Texture loading is async through the existing `loadTexture` seam — the terrain renders its unchanged vertex-colour look instantly and upgrades in place with one recompile (`Terrain.texturesReady`); quality gains a new `terrainDetail: "albedo" \| "full"` knob (low omits the normal-map block at BUILD TIME, no dangling sampler) plus `terrainAnisotropy` (4/4/8). JS-gzip landed at +3.0 KB (20.3 KB of the 400 KB cap left, inside the ~20 KB the design flagged as remaining). All 4 gates green (`build`/`test`/`check:bundle`/`verify` EXIT=0); visually confirmed via manual Playwright spot-checks at noon lighting — camp ground reads as real mottled jungle-floor texture with tint bands surviving, the lagoon shore shows a clean sand→jungle-floor blend right at the waterline, and the highland/overhang slopes read as mossy rock. Low tier (forced via `localStorage`) was also spot-checked and looks correct (albedo-only, no normal maps) — noted as a manual, non-CI check since no quality-override flag exists in the official verify tooling. review: lossy normal-map encoding + texture dispose gap — fixed. The normal maps were VP8 lossy (chroma-subsampled YUV smearing directional data into blotchy relief); re-encoded lossless (VP8L) via `scripts/process-textures.mjs`, at HALF resolution (512px, albedo stays 1024px) — full-resolution lossless alone would have blown the 6 MB total-download cap by ~3.7 MB, so the resolution trade is what makes the correct encoding affordable (texture payload 2701.0 → 3065.7 KB, total download 3187.6 → 3561.1 KB, still 2438.9 KB of headroom, `npm run check:bundle` EXIT=0). Separately, `Terrain.dispose()` only released geometry+material on the happy attach path, leaking the loaded textures (the unmount-race branch already disposed its own); `attachTerrainTextures` now tracks attached textures and `dispose()` releases them too, following `props.ts`'s explicit-texture-dispose convention — test-first (`terrain.textures.test.ts`, confirmed red before the fix, green after). CI render-gate caught low-tier texture cost on software GL — low tier now ships the vertex-color terrain (terrainDetail none/full); lint fix. |
| 4 — water | — | Replaced the fresnel-only, translucent-blue-sheet water ramp with ripple normal-map detail + physically-plausible depth-based colour absorption + a raggedized foam edge, ALL still one draw call/one mesh (`waterPatch.ts` idiom): two scrolling samples of one ripple-normal texture (1x + ~2.7x tile scale, headings baked as exact rational cos/sin pairs, scroll speeds derived to close the swell's own `WRAP_PERIOD` wrap seamlessly) blend additively onto the analytic two-sine wave normal in the FRAGMENT stage (per-pixel, so sun glints read sharp — the perturbed normal feeds three's own specular BRDF for free by anchor ordering); depth absorption (`1-exp(-depth*0.4)`) combines with fresnel via `max()` against a new, more-saturated tropical-turquoise/dark-teal detail palette; foam's smoothstep edges jitter from the same two ripple samples (no extra fetch); roughness drops 0.25→0.12 on the detail tier only. New `quality.waterDetail: "none"\|"full"` knob (low: none, medium/high: full, requires `waterDisplacement` also on — `wantDetail` ANDs all three defensively) follows the `terrainDetail` precedent exactly, including the async "render the base look now, upgrade once the texture attaches" `texturesReady` idiom (mirrors `Terrain`). Low tier verified BYTE-IDENTICAL: the four pre-existing `customProgramCacheKey` values are regression-locked (pinned exact strings in `waterPatch.test.ts`), and the render gate (forced low via software-GL) exercises exactly that untouched path. **Deviation:** the design doc assumed ambientCG stocks a CC0 water-ripple-normal set; it does not (verified against the live catalog — near-misses only, recorded in `public/assets/LICENSES.md`), so the ripple-normal texture is PROCEDURALLY GENERATED instead (a deterministic seeded sum of 28 sine terms, exactly tileable, zero third-party licensing) — 512x512 lossless WebP, 198.6 KB. Underwater dapple shipped as pure CSS (`::before` + `@keyframes` background-position drift on the existing DOM overlay) — zero WebGL cost, gated on both reduced-motion mechanisms already in this codebase. Planar reflections (`Reflector`) consciously SKIPPED — doubles the scene render, judged too expensive against the fill-rate-first mitigation order. JS-gzip landed at +0.9 KB (19.3 KB of the 400 KB cap left, was 20.2 KB after slice 3); total download +205.0 KB (texture payload + small deltas, 2233.9 KB of the 6 MB cap left). All 5 gates green (`lint`/`build`/`test`/`check:bundle`/`verify` EXIT=0). Visually confirmed via manual Playwright spot-checks (real GPU, `quality: "high"` forced via `localStorage`) at noon and golden-hour/dusk over both the lagoon and a river bend: ripple detail and shore foam breakup read clearly at both times of day, and the dusk river shot shows a distinct warm sun-glint reflection streak on the water (the "sharp glints / long soft reflections" goal); a real in-game submerged shot (swim → pointer-lock mouse-look pitch-dive) confirms the underwater DOM overlay and its `dapple-drift` animation are both live. self-caught pre-merge: a real WebGL compile (the manual visual-verification pass, not the headless suite) failed with `normalMatrix` "undeclared identifier" in the detail fragment block — it turns out to be part of three's VERTEX-only prefix, not shared with fragment; fixed by declaring `uniform mat3 normalMatrix;` in the fragment preamble too (`WebGLUniforms` binds by name across the whole linked program, so no further wiring was needed), now guarded by a `waterPatch.test.ts` regression test. The headless string tests alone would never have caught this (they never compile the GLSL) — the honest lesson recorded here for the next shader-owning slice: render the real thing before declaring a shader change done. review: missing water anisotropy + faceDirection mirror on detail normal — fixed. The ripple-normal texture never set `tex.anisotropy` (water is viewed at grazing angles almost the entire session — the worst case for the aniso=1 default); `terrainAnisotropy` was renamed to the shared `quality.textureAnisotropy` (one knob, both `terrain.ts` and `boundaries.ts` read it — same 4/4/8 tier values, no per-feature duplicate) and threaded into `attachWaterDetail`, pinned in `boundaries.textures.test.ts` exactly like `terrain.textures.test.ts:116-118` pins terrain's. Separately, the detail-normal perturbation in `waterPatch.ts` added its ripple tilt with an absolute sign, but the water material is `side: THREE.DoubleSide` (swimming/looking up at the surface) and three's `normal_fragment_begin` already flips the BASE normal by `faceDirection` on the back face before the patch runs — so the glint didn't mirror underwater; fixed by scaling the added term by `faceDirection` too (confirmed unconditionally declared in three 0.185's real chunk source, ahead of any `#ifdef DOUBLE_SIDED` branch, so no guard was needed), pinned in `waterPatch.test.ts`. All 5 gates green (`lint`/`build`/`test`/`check:bundle`/`verify` EXIT=0). |
| 5 — sky/atmosphere | — | Upgraded the flat 2-colour gradient dome to a Preetham/Rayleigh-flavoured atmosphere (`src/world/skyAtmosphere.ts` pure reference math → `sky.ts`'s `buildDomeMaterial` GLSL, the `waterSurface.ts` idiom): a horizon-haze band (`hazeFactor`), a sharp sun disc + broad Mie-style halo warming toward amber as the sun gets low (`sunDiscFactor`/`sunHaloFactor`/`lowSunFactor`), all driven by two NEW dome uniforms (`sunDirection`/`sunColor`) `DayCycleSystem` now writes alongside the existing `topColor`/`bottomColor` — same seam, no new clock. `EnvLightSystem`'s private bake-scene copy of the SAME `buildDomeMaterial` mutes the new disc/halo terms (`sunDiscStrength = 0`) so the calibrated PMREM energy budget (its own dedicated sun-glow disc mesh) is untouched — only the bounded gradient/haze upgrade reaches the bake. Fog density (`fogDensityForElevation`) now also retunes per phase (0.0022 at noon, bit-exact; +~30% at the lowest-sun keyframes) instead of one flat value, agreeing with the dome's own horizon look. Added a seeded ~1200-star `Points` field (`src/world/starfield.ts`, ALL tiers, 1 draw call) fading in on a twilight curve keyed to sun elevation (this world's day cycle has NO literal night — `dayCycle.ts`'s documented floor never dips below elevation 0.12 — so stars read as twilight/pre-dawn, strongest at the dawn keyframe, not a literal midnight sky; DEVIATION recorded and visually confirmed) and wheeling slowly around a fixed pole (frozen under reduced motion, no twinkle either). Added 7 drifting billboard clouds (`src/world/clouds.ts`, medium/high via new `quality.cloudDetail`, 1 draw call): a procedurally-baked soft-puff canvas texture (the `makeLeafTexture` idiom, no download), seeded scatter + wrap-around drift, full camera-facing billboard (`camera.quaternion` copied straight onto each of the ~7 instances — cheap at this count), tinted from bright noon-white toward the sun colour and a modest darken at low sun (shares `lowSunFactor` with the dome). God rays (high tier only) WORKED: pmndrs `GodRaysEffect` merged into the SAME `EffectPass` as bloom/SMAA/vignette/tone-mapping (`createCompositor.ts`), light-sourced by a small sphere repositioned along the live sun direction every frame and faded via the merged pass's own per-effect blend opacity (0 at noon, up to 0.6 at low sun — a small local duplicate of `lowSunFactor`, kept engine-layer-only rather than importing `src/world`). Measured bundle cost was the headline finding: only **+1.35 KB gzip** (postfx 151.07→152.33 KB, createCompositor split +0.09 KB) — GodRaysEffect shares most of its machinery with what the chain already imports, far cheaper than the ~19 KB headroom left after slice 4 suggested was at risk; no fallback/skip was needed. Total slice cost: entry chunk 95.68→98.03 KB gz (+2.35 KB, the 5 new pure/GPU modules pulled in eagerly by every tier), `postfx` chunk 151.07→152.33 KB gz (+1.26 KB, god rays only), summed JS-gzip 380.7→**384.6 KB** (**15.4 KB** of the 400 KB cap left), total download 3766.1→**3770.1 KB** (+4.0 KB, no new binary assets — everything is procedural). Draw calls: +1 on every tier (stars) +1 more on medium/high (clouds), exactly the design's `+2 max` budget; god rays adds zero draw calls (postfx). All 5 gates green (`lint`/`build`/`test`/`check:bundle`/`verify` EXIT=0, the render-gate's forced-low/SwiftShader run exercising exactly the stars-only, no-clouds-no-godrays path). Visually confirmed via manual Playwright spot-checks (real GPU, `quality` forced via `localStorage`) at dawn/noon/dusk/~150s("night") on both low and high: low shows a warm horizon gradient + a visible sun disc/halo at dusk/dawn and a clearly readable star field with none of the extra draws; high additionally shows soft white-to-ember-tinted clouds drifting and a faint god-ray glow near the sun at dawn/dusk/reduced-motion-golden-hour, near-invisible at noon — the IBL-lit ground/tent read correctly lit at every phase on both tiers, no regression. **Correction, HONEST:** that original visual pass saw the glow but never verified the shaft actually TRACKED the sun's arc across dawn/dusk — it did not. review: god-rays light frozen at origin (pmndrs matrixAutoUpdate trap) + unwrapped star uTime — fixed, docs corrected. The god-rays light-source mesh's `matrixWorld` was frozen at its construction-time identity forever (pmndrs `GodRaysEffect.update()` forces `matrixAutoUpdate = false` immediately before calling `updateWorldMatrix`, whose own guard then skips `updateMatrix()` entirely — reproduced empirically: three successive `position.copy()` writes, `matrixWorld` printed `(0,0,0)` every time), so only the opacity fade ever worked; fixed by computing the mesh's `matrix`/`matrixWorld` directly inside `buildGodRays`'s `update()` every frame, test-first (`createCompositor.test.ts`, confirmed red before the fix). Separately, `StarfieldSystem`'s `elapsed` accumulator fed an ever-growing value into `uTime`/`group.rotation.y` with no wrap, violating this codebase's own float32-precision discipline (`waterSystem.ts`'s `WRAP_PERIOD` precedent); fixed by wrapping modulo a derived `STAR_WRAP_PERIOD = 1000π` (the smallest period at which both the twinkle rate 1.6 rad/s and the rotation rate 0.006 rad/s close on a whole number of 2π cycles — 800 and 3 respectively), pinned by wrap-continuity tests. Re-verified visually at dawn AND dusk on high tier post-fix: the shaft now genuinely anchors at the sun disc's screen position in both. All 5 gates green post-fix (`lint`/`build`/`test`/`check:bundle`/`verify` EXIT=0). |
| 6 — flora & fauna models | — | |
| 7 — polish + live verify | — | |
