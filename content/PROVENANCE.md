# Content provenance

**Current payload:** `expedition.json` — the clue chain for **The Lost Idol**
(pivot 2026-07-08). It is fiction, authored for the game: the field notes of a
vanished expedition, written so each page locates the next site by describing
real landmarks in the generated world (`src/world/worldConfig.ts` anchors).
There are no factual claims to ground.

The previous payload (`working-with-claude.json`, the 13-landmark "about me"
dataset and its per-POI grounding table) was retired with the pivot; it
survives in git history (pre-pivot `main`) and the run logs under
`docs/team/runs/`.
